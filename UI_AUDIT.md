# UI Audit

Date: 2026-06-04

Scope: 发现页、生活圈页、匹配页、推荐结果、邀约弹窗、消息列表、聊天页、换地点/排队弹窗、成局页、团购券弹窗、我的页。

## Findings

| ID | Severity | Area | UI problem | Status |
| --- | --- | --- | --- | --- |
| UI-01 | High | 全局 Header | `重置` 是演示控件，普通用户态在聊天/成局/我的页可见，破坏产品感。 | Fixed |
| UI-02 | High | 匹配结果 / 聊天页 | `开发者模式` 按钮以文字按钮出现在主界面，视觉层级过高，用户第一眼会误以为是主要功能。 | Fixed |
| UI-03 | High | 匹配结果页 | 结果页同时展示输入框、最近偏好、灵感推荐、候选人列表、AI 总结、方案卡，信息密度过高，主任务“选方案”不够突出。 | Fixed |
| UI-04 | High | 邀约弹窗 | 弹窗仍是旧式 emoji + 黄底虚线框，和新版卡片/底部弹窗体系不一致。 | Fixed |
| UI-05 | High | 成局页 | 3 个主按钮并列且都用主色，行动优先级不清楚；履约信息像表格堆叠，视觉层级弱。 | Fixed |
| UI-06 | Medium | 团购券弹窗 | 普通用户态出现 `AI Rank`、综合分等算法表达，卡片信息像调试面板。 | Fixed |
| UI-07 | Medium | 我的页 | `开发者：数据健康度` 在普通用户态可见，是内部信息。 | Fixed |
| UI-08 | Medium | 聊天页候补卡 | “拒人重算（B → 候补）”和打分拆解是内部语言，用户态信息表达生硬。 | Fixed |
| UI-09 | Medium | 匹配方案卡 | 每张方案卡文本偏长，二级信息和主 CTA 间距接近，扫读成本高。 | Fixed |
| UI-10 | Low | 生活圈页 | 信息量很丰富但区块较多，后续可进一步收缩筛选项；当前不阻断主流程。 | Deferred |
| UI-11 | Medium | 换地点 / 排队弹窗 | 普通用户态出现排序、分数、公式和候选维度拆解，像算法面板而不是用户决策弹窗。 | Fixed |
| UI-12 | Medium | 价格 / 团购文案 | 同一页面同时出现 `¥90` 和 `$32`，货币系统不统一。 | Fixed |
| UI-13 | Low | Toast / 聊天元信息 | 普通用户态出现“演示态”“模拟对方”等内部提示，影响产品沉浸感。 | Fixed |

## Resolution Notes

- 普通入口默认隐藏演示控件、开发者按钮、调试字段、算法公式和内部状态命名；`?dev=1` 保留开发者演示能力。
- 匹配结果页进入结果态后聚焦方案列表，隐藏输入框、最近偏好、灵感推荐和候选人列表。
- 邀约、安全、诚意金、团购券、换店弹窗统一为底部 sheet 体系，去掉旧 emoji / 黄底虚线 / 调试卡片感。
- 成局页只保留一个主行动，其余按钮降级为次级；履约步骤改成时间线。
- 团购和方案卡金额统一为人民币符号。

## Design Rules Applied

- 普通用户态不展示演示、调试、算法公式、内部层级命名。
- 一个页面最多突出一个主行动；其他行动降为次级。
- 结果页优先展示“推荐方案”，减少与决策无关的前置模块。
- 弹窗统一为底部 sheet + 白卡 + 克制主色按钮。
- 文案从内部系统语言改成用户能理解的行动语言。
